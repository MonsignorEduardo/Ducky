{
  "name": "my-turborepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:all": "turbo run build",
    "build:telegram-bot": "turbo run build --filter telegram-bot",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "postinstall": "prisma generate",
    "db:push": "prisma db push",
    "db:seed": "prisma db seed",
    "start:telegram-bot": "turbo run start --filter telegram-bot"
  },
  "dependencies": {
    "@prisma/client": "^4.3.1"
  },
  "devDependencies": {
    "prettier": "latest",
    "prisma": "^4.3.1",
    "turbo": "^1.5.5",
    "typescript": "^4.8.4"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.11.0"
}
